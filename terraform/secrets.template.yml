database_url: "psql://${database.username}:${database.password}@${database.endpoint}/${database.name}"
aws_access_key_id: "${access_key.id}"
aws_secret_access_key: "${access_key.secret}"
aws_storage_bucket_name: "${s3_bucket.id}"
