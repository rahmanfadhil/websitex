build:
  docker:
    web: Dockerfile
run:
  worker:
    command:
      - python manage.py rqworker
    image: web
  scheduler:
    command:
      - python manage.py rqscheduler
    image: web
release:
  image: web
  command:
    - python manage.py migrate --noinput
