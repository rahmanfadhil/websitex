-r base.txt

# The web server which the app runs on in production
# https://github.com/django/daphne
daphne==3.0.2

# Persist user-uploaded content to S3
# https://github.com/jschneier/django-storages
django-storages==1.11.1

# Django email backends to send emails
# https://github.com/anymail/django-anymail
django-anymail[amazon_ses]==8.4

# The AWS SDK for Python which `storages` and `anymail` depend on
# https://github.com/boto/boto3
boto3==1.18.16

# Log and debug errors in production
# https://github.com/getsentry/sentry-python
sentry-sdk==1.3.1
